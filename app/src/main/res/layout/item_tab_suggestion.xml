<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <variable
            name="suggestion"
            type="com.example.videosaver.advance.data.local.room.entity.HistoryItem" />

        <variable
            name="listener"
            type="com.example.videosaver.advance.ui.component.adapter.SuggestionTabListener" />

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/tv_suggestion"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:drawableStart="@drawable/public_24px"
            android:drawablePadding="@dimen/_8sdp"
            android:gravity="center_vertical"
            android:maxLines="2"
            android:onClick="@{() -> listener.onItemClicked(suggestion)}"
            android:padding="@dimen/_8sdp"
            android:textSize="14sp"
            tools:text="@{suggestion.url}" />

    </LinearLayout>

</layout>